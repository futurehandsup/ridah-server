<% include ../templates/head.ejs %>
<!-- Modal -->
  
  <!--통계페이지-->

 <div class="container-fluid">
    <h2 class="my-3">부가세 신고 내역</h2>
    <div class="card">

    <div class="card" style="margin-bottom: 20px;">
      <ul class="list-group list-group-flush">
        
          <li class="list-group-item">부가세 신고 
            <span style="float: right">
              <button>동영상 교육보기</button>
            </span>
          </li>
          
      </ul>
    </div>

    <div class="card" style="margin-bottom: 20px;">
      <div class="card-header">
        조회하기   
        <!--css등을 이용하여 버튼으로-->
        <form>
          <input type="date" name="date-from" value="<%=query.date_from%>" class="form-control"> 
          <input type="date" name="date-until" value="<%=query.date_until%>" class="form-control">
          <button>검색</button>
          <button>초기화</button>
        </form>

      </div>   
    </div> 

    <div class="card" style="margin-bottom: 20px;">
      <div class="card-header">
       <table>
          <tr>
            <td>조회결과</td>
          </tr>
        </table>
      </div>
      <ul class="list-group list-group-flush">
        <li class="list-group-item">월별 내역</li>
        <li class="list-group-item">그래프가 없습니다</li>
        <li class="list-group-item">월간 상세 내역</li>
        <li class="list-group-item">그래프가 없습니다</li>
      </ul>
    </div>
    </div>
</div>

<% include ../templates/bottom.ejs %>
<script>
$(document).ready(function(){
  $('#createButton').click(function(){
    var program =  $('#createForm').serializeObject();
    program.store = '<%=store.id%>';

    $.ajax({
      type:"POST",
      url:'/programs',
      data:program,
      success:function(args){
        alert("성공했습니다.");
        location.reload();
      },
      error:function(e){
          alert(e.responseText);
      }
    });
  });
  $('.deleteButton').click(function(){
    var programId = $(this).data("item");
    $.ajax({
      type:"DELETE",
      url:'/programs/'+programId,

      success:function(args){
        alert("성공했습니다.");
        location.reload();
      },
      error:function(e){
          alert(e.responseText);
      }
    });
  });
});
</script>
<script>
$(document).ready(function(){
  $('.sidebar .nav .active').removeClass('active');
  var path = "calculations-vat" ;
  $('#'+path).addClass('active');
})
</script>
