<% include ../templates/head.ejs %>
<!-- Modal -->
  
  <!--통계페이지-->

 <div class="container-fluid">
    <h2 class="my-3">정산 정보</h2>
    <div class="card">

    <div class="card" style="margin-bottom: 20px;">
      <ul class="list-group list-group-flush">
        
          <li class="list-group-item">그래프가 없습니다</li>
        
      </ul>
    </div>

    <div class="card" style="margin-bottom: 20px;">
      <div class="card-header">
        정산예정일     
        <!--css등을 이용하여 버튼으로-->
        <input type="date" name="date-from"> 
        <input type="date" name="date-until">
      </div>   
    </div> 

    <div class="card" style="margin-bottom: 20px;">
      <div class="card-header">
       <table>
          <tr>
            <td rowspan="2">정산예정일</td>
            <td rowspan="2">정산완료일</td>
            <td rowspan="2">판매자 정산번호</td>
            <td rowspan="2">정산금액(A+B+C+D+E+F)</td>
            <td colspan="6">상세내역</td>
            <td rowspan="2">정산방식</td>
            <td rowspan="2">정산계좌</td>
          </tr>
          <tr>
            <td>결제금액(A)</td>
            <td>수수료합계(B)</td>
            <td>혜택정산(C)</td>
            <td>일별공제환급(D)</td>
            <td>지급보류(E)</td>
            <td>마이너스충전금 상계(F)</td>
          </tr>
        </table>
      </div>
      <ul class="list-group list-group-flush">
        
          <li class="list-group-item">그래프가 없습니다</li>
      </ul>
    </div>
    </div>
</div>

<% include ../templates/bottom.ejs %>
<script>
$(document).ready(function(){
  $('#createButton').click(function(){
    var program =  $('#createForm').serializeObject();
    program.store = '<%=store.id%>';

    $.ajax({
      type:"POST",
      url:'/programs',
      data:program,
      success:function(args){
        alert("성공했습니다.");
        location.reload();
      },
      error:function(e){
          alert(e.responseText);
      }
    });
  });
  $('.deleteButton').click(function(){
    var programId = $(this).data("item");
    $.ajax({
      type:"DELETE",
      url:'/programs/'+programId,

      success:function(args){
        alert("성공했습니다.");
        location.reload();
      },
      error:function(e){
          alert(e.responseText);
      }
    });
  });
});
</script>
<script>
$(document).ready(function(){
  $('.sidebar .nav .active').removeClass('active');
  var path = "calculations-info" ;
  $('#'+path).addClass('active');
})
</script>
